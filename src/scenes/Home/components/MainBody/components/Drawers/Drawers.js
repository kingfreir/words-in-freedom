import React, { Component } from 'react'
import styles from './Drawers.css'

class Drawers extends Component {
  render() {
    return (
      <div className={`${styles.drawers} color`}/>
    )
  }
}

export default Drawers